package com.keepcoding.appfinalKC;

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

/*public class GenerarPassword {

	public static void main(String[] args) {
		BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();
        String rawPassword = "password";
        String hashedPassword = encoder.encode(rawPassword);
        System.out.println("Contraseña en texto plano: " + rawPassword);
        System.out.println("Contraseña encriptada: " + hashedPassword);
	}

}
	- Método usado para codificar la contraseña 'password' y pegar el hash generado en consola en import.sql. Lo dejo como comentario de texto para que al ejecutar la app no te pida ejecutar esta.
*/

